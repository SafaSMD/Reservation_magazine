apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: example-ingress
  annotations:
#    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
    - host: magazine1.info
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: magazine1
                port:
                  number: 8080